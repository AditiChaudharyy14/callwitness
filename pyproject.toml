[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "callwitness"
version = "0.1.1"
description = "Record every tool call an AI agent makes. Block nothing."
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }
authors = [{ name = "Aditi Chaudhary" }]
keywords = ["ai", "agents", "mcp", "security", "observability", "ai-safety"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Security",
    "Topic :: System :: Monitoring",
]
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[project.scripts]
callwitness = "callwitness.cli:entrypoint"

[project.urls]
Homepage = "https://github.com/AditiChaudharyy14/callwitness"
Source = "https://github.com/AditiChaudharyy14/callwitness"

[tool.hatch.build.targets.wheel]
packages = ["src/callwitness"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = ["slow: end-to-end runs that spawn processes"]
